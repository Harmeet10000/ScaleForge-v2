/**
 * tests/unit/health.test.ts
 *
 * DEPRECATED: This file tested legacy Express health utils from src/utils/quicker.
 * The quicker utils module was removed as part of the Fastify/Effect migration.
 * See tests/unit/features/health.test.ts for the current health tests.
 */

import { describe, it, expect } from "bun:test"

describe("Health Utils (legacy — superseded)", () => {
  it("placeholder — see tests/unit/features/health.test.ts", () => {
    expect(true).toBe(true)
  })
})
